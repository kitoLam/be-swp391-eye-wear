version: '3.8'

services:
    backend:
        build: .
        container_name: eyewear-backend
        ports:
            - '5000:5000'
        environment:
            - NODE_ENV=production
        env_file:
            - .env
        restart: always
